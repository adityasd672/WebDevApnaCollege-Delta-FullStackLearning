<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your post</title>
</head>
<body>
    <h2>Edit your post</h2>
    <p>Post id: <%= post.id %></p>
    <p>Post username: <%= post.username %></p>
    <% if(post.imgSrc) { %>
        <img src="<%= post.imgSrc %>" alt="Post image" width="300">
    <% } %>

    <form method="post" action="/posts/<%= post.id %>?_method=PATCH">
        <textarea name="caption" id="caption" rows="10" cols="35"><%= post.caption %></textarea>
        <button>Submit</button>
    </form>
</body>
</html>